package com.storm.safe.rock

/**
 * Obfuscated import registry — holds references to obfuscated classes
 * across multiple packages (androidx.compose, androidx.core, androidx.room, etc.).
 *
 * Reverse-engineered from JADX: JunkRegistry.java (1516 lines).
 * ADAPT: obfuscated import holder, skeleton only.
 *
 * In the original APK, this class contained ~1500 lines of static import references
 * to obfuscated classes with names like AbstractObserverProxy1430, AsyncCallbackManager1432,
 * etc. These classes are dead code / anti-tamper padding and do not contain meaningful logic.
 *
 * The companion object preserves the class structure for reference.
 */
class JunkRegistry {

    companion object {
        private const val TAG = "JunkRegistry"

        // ADAPT: obfuscated import holder, skeleton only
        // Original file contained ~1500 lines of import references to:
        // - androidx.compose.material.icons.*  (24 classes)
        // - androidx.compose.ui.platform.*     (22 classes)
        // - androidx.compose.runtime.internal.* (18 classes)
        // - androidx.core.internal.*            (20 classes)
        // - androidx.room.migration.bundle.*    (22 classes)
        // - ...and many more obfuscated packages
        //
        // All references are to empty/junk classes with naming patterns like:
        //   AbstractObserverProxy1430, AsyncCallbackManager1432,
        //   CachedChainWrapper1427, DefaultProvider1428, etc.
        //
        // These serve as anti-decompilation padding and are not functionally used.

        /**
         * Placeholder constant to keep the class loadable.
         */
        const val REGISTRY_VERSION = 1
    }
}
