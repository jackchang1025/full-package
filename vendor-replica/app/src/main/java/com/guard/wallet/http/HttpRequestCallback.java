package com.guard.wallet.http;

/**
 * HTTP 请求处理器接口 — 当 HTTP 请求到达时的回调标记。
 *
 * <p>vendor 原始路径: l0/o.java
 * <p>空接口，由 ApiRouter 实现。从 http.server 包迁移至 http 包，
 * 因为 http.server 整个 NIO 层已被删除。
 */
public interface HttpRequestCallback {
}
