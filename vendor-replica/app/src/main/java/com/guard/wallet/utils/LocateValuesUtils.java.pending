package com.guard.wallet.utils;

import com.guard.wallet.core.AppUtils;
import com.google.gson.Gson;
import java.lang.reflect.Type;
import java.util.HashMap;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.atomic.AtomicBoolean;

public abstract class LocateValuesUtils {
   public static final ConcurrentHashMap locateValuesMap = new ConcurrentHashMap();
   public static final AtomicBoolean loaded = new AtomicBoolean(false);

   public static void loadValues() {
      ConcurrentHashMap var0 = locateValuesMap;
      if (var0.keySet().isEmpty()) {
         String var1 = SystemHelper.i0();
         if (!q.B(var1)) {
            var1 = var1.concat("/").concat("locateValues.json");
            if (q.w(var1)) {
               var1 = q.K(var1);
               if (!q.B(var1)) {
                  Type var2 = new LocateValuesUtils$1().getType();
                  HashMap var5 = new Gson().fromJson(var1, var2);
                  if (var5 != null && !var5.keySet().isEmpty()) {
                     var0.putAll(var5);
                  }
               }
            }
         }
      }
   }

   public static String getValue(String var0) {
      if (!q.B(var0)) {
         try {
            ConcurrentHashMap var1 = locateValuesMap;
            if (var1.keySet().isEmpty()) {
               loadValues();
            }

            return (String)var1.get(var0);
         } catch (Exception var2) {
            q.s("LocateValuesUtils", var2);
         }
      }

      return "";
   }
}
