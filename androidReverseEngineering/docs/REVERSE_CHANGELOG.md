# 逆向工程变更日志

本文件记录所有反编译注解操作，按日期倒序排列。

---

## 2026-04-04

| 文件 | 操作 | 说明 |
|------|------|------|
| a1/q.java | ANNOTATE | 35个方法全部添加功能语义注释；识别加密/日志/文件/网络/UI自动化/电池/对象池等功能域 |

### 注解详情

**已识别功能域（a1/q.java）：**

| 功能域 | 方法列表 |
|--------|---------|
| 空值检查 | `B()` |
| AES 解密 | `m()` |
| 日志工具 | `s()`, `t()` |
| Shell 命令执行 | `u()` — JADX 反编译失败，见字节码注释 |
| TLS/SSLContext 构建 | `y()` |
| 字符串清理 | `Q()` |
| 数字字符串判断 | `D()` |
| 端口可用性检测 | `E()` |
| 时间格式化 | `H()` |
| HTTP 方法判断 | `I()` |
| 文件读取（Bitmap）| `J()` |
| 文件读取（文本）| `K()` — JADX 反编译失败 |
| 文件写入 | `U()` |
| 文件创建 | `l()` |
| 文件删除 | `n()` |
| 文件存在检查 | `w()`, `v()` (frpc.ini) |
| 文件名提取 | `x()` |
| 对象池 push | `L()` |
| 对象池 pop | `P()` |
| Base64 编码 | `r()`, `q()` |
| 电池信息读取 | `d()` |
| Root 检测 | `e()` |
| 屏幕唤醒 | `S()` |
| 无障碍前台检查 | `A()` |
| 前台包名检查 | `C()` |
| 悬浮窗权限检查 | `G()` |
| 启动器图标查找+点击 | `O()` |
| 等待前台就绪 | `R()`, `b()` |
| Bridge 连接关闭 | `g()` |
| Bridge 连接建立 | `k()` |
| 流推送（Buffer）| `F()` |
| 缓存任务执行 | `N()` |
| 数据管道处理 | `p()` |
| ByteChannel 写入 | `c()` — JADX 反编译失败 |
| NIO 数据写入 | `T()` |
| UI 过滤器构建 | `o()` |
| Bundle 构建 | `a()` |
| Closeable 批量关闭 | `h()` |

**无法确定功能的方法：** 无（所有方法均已推断）

---

_由逆向注解工具自动生成，2026-04-04_
